{{> header}}
<div class="container">
    <div class="row">
        <div class="col-lg-6 col-sm-6 col-xs-6">
            <div class="jumbotron" id="modif-jumbotron">
                {{> loginForm}}
                <a class="btn btn-primary" id="fbBtn" href="#/loginF">Login with Facebook</a>
            </div>
        </div>
    </div>
</div>
{{> footer}}